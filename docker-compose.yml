db:
    image: mongo
    ports:
      - "27018:27017"
    command: "--smallfiles --logpath=/dev/null"
web:
    build: .
    command: "node index.js"
    volumes:
        - ./app:/myapp
        - /myapp/node_modules